# Cats-and-Dogs-Classification-Project
Developed a convolutional neural network (CNN) model to classify images of cats and dogs. Applied data  preprocessing techniques, optimized the model for performance, and achieved high accuracy in  distinguishing between the two classes using image classification methods
